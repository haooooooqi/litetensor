INCLUDE_DIRECTORIES(${LITETENSOR_HEADER_DIR})
INCLUDE_DIRECTORIES(${MPI_CXX_INCLUDE_PATH})
INCLUDE_DIRECTORIES(${EIGEN3_HEADER_DIR})

# Executable
AUX_SOURCE_DIRECTORY(./ APPLICATION_SRC)
ADD_EXECUTABLE(litetensor ${APPLICATION_SRC})

# MPI
TARGET_LINK_LIBRARIES(litetensor ${MPI_CXX_LIBRARIES})
